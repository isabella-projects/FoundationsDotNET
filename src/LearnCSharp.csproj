<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net8.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>

        <!-- Conditional compilation of some functions -->
        <!-- Some of the functions I don't want to be compiled -->
        <DefineConstants>RETURN_ARRAY_METHOD;DICE_MINI_GAME;PETTING_ZOO;MINI_GAME</DefineConstants>
    </PropertyGroup>

</Project>